// Config & Setup
const config = {
    type: Phaser.AUTO,
    width: 1280,
    height: 720,
    backgroundColor: 0x660000
};

const game = new Phaser.Game(config);

// Debugging purposes
console.log(game);